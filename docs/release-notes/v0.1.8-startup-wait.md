# 启动等待优化版

基于 [inga-lovinde/RadeonResetBugFix](https://github.com/inga-lovinde/RadeonResetBugFix) `v0.1.7` 修改。

## 变更

- 将启动阶段固定的 40 秒等待改为等待 `BasicDisplay` 兜底显示驱动就绪。
- 将固定的 20 秒等待改为等待 AMD 显卡就绪。
- 保留原超时时间作为兜底，异常情况下仍不会无限等待。

## 说明

- 本版本只包含启动等待优化，不包含重启旁路相关改动。
- 已在 Windows 10 虚拟机中验证。
